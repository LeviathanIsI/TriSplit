<UserControl x:Class="TriSplit.Desktop.Views.Tabs.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             AllowDrop="True"
             PreviewDragOver="ProfilesView_OnPreviewDragOver"
             Drop="ProfilesView_OnDrop"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1400"
             Background="{StaticResource BackgroundBrush}">

  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
  </UserControl.Resources>

  <Grid Margin="20">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="340"/>
      <ColumnDefinition Width="20"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Grid.Column="0"
            Grid.ColumnSpan="3"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="16"
            Margin="0,0,0,16">
      <Grid>
        <Grid.ColumnDefinitions>
                                  <ColumnDefinition Width="*"/>

                                  <ColumnDefinition Width="Auto"/>

                                  <ColumnDefinition Width="Auto"/>

                                </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
          <TextBlock Text="Data Profile Configuration"
                     Style="{StaticResource HeaderTextBlock}"
                     Foreground="{StaticResource AccentBrush}"/>
          <TextBlock Text="Map source data fields to HubSpot properties"
                     Foreground="{StaticResource SecondaryTextBrush}"/>
        </StackPanel>

        <StackPanel Grid.Column="1"
                    Orientation="Vertical"
                    HorizontalAlignment="Right"
                    MinWidth="340">
          <TextBlock Text="Profile Name"
                     Foreground="{StaticResource SecondaryTextBrush}"/>
          <TextBox Text="{Binding ProfileName}"
                   Margin="0,4,0,8"
                   VerticalAlignment="Center"/>

          <CheckBox Content="Create Secondary Contacts File"
                    IsChecked="{Binding CreateSecondaryContactsFile}"
                    Foreground="{StaticResource TextBrush}"
                    Margin="0,0,0,12"/>

          <StackPanel Orientation="Horizontal">
            <Button Content="Save"
                    Command="{Binding SaveProfileCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Padding="14,6"
                    Margin="0,0,8,0"/>
            <Button Content="Save As"
                    Command="{Binding SaveProfileAsCommand}"
                    Style="{StaticResource SecondaryButton}"
                    Padding="14,6"
                    Margin="0,0,8,0"/>
            <Button Content="Load"
                    Command="{Binding LoadProfileCommand}"
                    Style="{StaticResource SecondaryButton}"
                    Padding="14,6"
                    Margin="0,0,8,0"/>
            <Button Content="Suggest Headers"
                    Command="{Binding LoadHeaderSuggestionsCommand}"
                    Style="{StaticResource SecondaryButton}"
                    Padding="14,6"/>
          </StackPanel>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Saved Profiles -->
    <Border Grid.Column="0"
            Grid.Row="1"
            Grid.RowSpan="2"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="4">
      <Grid Margin="12">
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox ItemsSource="{Binding SavedProfiles}"
                 SelectedItem="{Binding SelectedProfile}"
                 BorderThickness="0"
                 Background="Transparent">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Border Padding="6" Margin="0,0,0,4" Background="{StaticResource BackgroundBrush}" CornerRadius="3">
                <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}"/>
              </Border>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
          <Button Content="New Data Profile"
                  Command="{Binding NewProfileCommand}"
                  Style="{StaticResource SecondaryButton}"
                  Margin="0,0,0,6"/>
          <Button Content="Delete Selected Data Profile"
                  Command="{Binding DeleteProfileCommand}"
                  Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Main Panel -->
    <Border Grid.Column="2"
            Grid.Row="1"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="4">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Group Defaults -->
        <Border Grid.Row="0"
                Background="{StaticResource BackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="16">
          <Expander IsExpanded="{Binding IsGroupDefaultsExpanded, Mode=TwoWay}"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    ExpandDirection="Down"
                    Padding="0">
                        <Expander.Header>

              <TextBlock Text="Group Defaults"

                         Foreground="{StaticResource AccentBrush}"

                         FontWeight="SemiBold"

                         FontSize="16"/>

            </Expander.Header>
                        <Expander.Content>
              <StackPanel>
                <Grid Margin="0,0,0,12">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="260"/>
                  </Grid.ColumnDefinitions>

                  <StackPanel Grid.Column="1">
                    <TextBlock Text="Missing Header Behavior"
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                    <ComboBox ItemsSource="{Binding MissingHeaderBehaviorOptions}"
                              SelectedItem="{Binding SelectedMissingHeaderBehavior}"
                              Margin="0,4,0,0"/>
                  </StackPanel>
                </Grid>

                <ItemsControl ItemsSource="{Binding GroupCollections}"
                              Margin="0,12,0,0">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                      <UniformGrid Columns="3"/>
                    </ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                      <Setter Property="Margin" Value="0,0,12,12"/>
                    </Style>
                  </ItemsControl.ItemContainerStyle>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Border Background="{StaticResource SurfaceBrush}"
                              BorderBrush="{StaticResource BorderBrush}"
                              BorderThickness="1"
                              CornerRadius="4"
                              Padding="12">
                        <StackPanel>
                          <Grid>
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="*"/>
                              <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="{Binding Header}"
                                       Foreground="{StaticResource SecondaryTextBrush}"
                                       FontWeight="SemiBold"/>

                            <Button Grid.Column="1"
                                    Content="+ Add Group"
                                    Command="{Binding AddGroupCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="10,4"/>
                          </Grid>

                          <ItemsControl ItemsSource="{Binding Groups}"
                                        Margin="0,8,0,0">
                            <ItemsControl.ItemTemplate>
                              <DataTemplate>
                                <Border Background="{StaticResource BackgroundBrush}"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="10"
                                        Margin="0,8,0,0">
                                  <Grid>
                                    <Grid.RowDefinitions>
                                      <RowDefinition Height="Auto"/>
                                      <RowDefinition Height="Auto"/>
                                      <RowDefinition Height="Auto"/>
                                      <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                      <ColumnDefinition Width="*"/>
                                      <ColumnDefinition Width="Auto"/>
                                      <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0"
                                               Grid.Column="0"
                                               Text="{Binding Title}"
                                               Foreground="{StaticResource AccentBrush}"
                                               FontWeight="SemiBold"/>

                                    <Button Grid.Row="0"
                                            Grid.Column="1"
                                            Content="Clone"
                                            Command="{Binding DataContext.CloneGroupCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="8,4"
                                            Margin="8,0,0,0"/>

                                    <Button Grid.Row="0"
                                            Grid.Column="2"
                                            Content="Remove"
                                            Command="{Binding DataContext.RemoveGroupCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="8,4"
                                            Margin="8,0,0,0"/>

                                    <StackPanel Grid.Row="1"
                                                Grid.ColumnSpan="3"
                                                Margin="0,8,0,0">
                                      <TextBlock Text="Association Label"
                                                 Foreground="{StaticResource SecondaryTextBrush}"/>
                                      <ComboBox ItemsSource="{Binding AssociationLabelOptions}"
                                                SelectedItem="{Binding AssociationLabel, Mode=TwoWay}"
                                                Margin="0,4,0,0"
                                                IsEditable="False"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2"
                                                Grid.ColumnSpan="3"
                                                Margin="0,8,0,0">
                                      <TextBlock Text="Data Source"
                                                 Foreground="{StaticResource SecondaryTextBrush}"/>
                                      <TextBox Text="{Binding DataSource}"
                                               Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="3"
                                                Grid.ColumnSpan="3"
                                                Margin="0,8,0,0">
                                      <TextBlock Text="Tags (comma separated)"
                                                 Foreground="{StaticResource SecondaryTextBrush}"/>
                                      <TextBox Text="{Binding Tags}"
                                               Margin="0,4,0,0"/>
                                    </StackPanel>
                                  </Grid>
                                </Border>
                              </DataTemplate>
                            </ItemsControl.ItemTemplate>
                          </ItemsControl>
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Expander.Content>
          </Expander>
        </Border>
        <!-- Column Headers -->
        <Border Grid.Row="1"
                Background="{StaticResource SelectedBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="14">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="2*"/>
              <ColumnDefinition Width="1.2*"/>
              <ColumnDefinition Width="2*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="SOURCE DATA HEADER"
                       Foreground="{StaticResource AccentBrush}"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="1"
                       Text="GROUP"
                       Foreground="{StaticResource AccentBrush}"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="2"
                       Text="HUBSPOT HEADER"
                       Foreground="{StaticResource AccentBrush}"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="3"
                       Text="ACTION"
                       HorizontalAlignment="Center"
                       Foreground="{StaticResource AccentBrush}"
                       FontWeight="SemiBold"/>
          </Grid>
        </Border>

        <!-- Mapping Rows -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      Padding="16">
          <ItemsControl ItemsSource="{Binding FieldMappings}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Border Background="{StaticResource BackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="12"
                        Margin="0,0,0,8">
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="2*"/>
                      <ColumnDefinition Width="1.2*"/>
                      <ColumnDefinition Width="2*"/>
                      <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding SourceField}"
                             Margin="0,0,12,0"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,12,0">
                      <ComboBox ItemsSource="{Binding DataContext.ObjectTypes, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                SelectedItem="{Binding ObjectType}"
                                Width="120"/>
                      <ComboBox ItemsSource="{Binding AvailableGroupIndices}"
                                SelectedItem="{Binding GroupIndex}"
                                Width="80"
                                Margin="8,0,0,0"/>
                    </StackPanel>

                    <ComboBox Grid.Column="2"
                              ItemsSource="{Binding DataContext.HubSpotHeaders, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                              SelectedItem="{Binding HubSpotHeader}"
                              IsEditable="True"
                              Margin="0,0,12,0"/>

                    <Button Grid.Column="3"
                            Content="Remove"
                            Command="{Binding DataContext.RemoveMappingCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"
                            Style="{StaticResource SecondaryButton}"
                            Padding="10,4"/>
                  </Grid>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>

        <!-- Mapping Summary -->
        <Border Grid.Row="3"
                Background="{StaticResource BackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1,0,0,0"
                Padding="14">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
              <TextBlock Text="{Binding MappingSummary}"
                         Foreground="{StaticResource SecondaryTextBrush}"/>
              <Border Background="#FF4C61"
                      CornerRadius="12"
                      Padding="8,2"
                      Margin="12,0,0,0"
                      Visibility="{Binding HasDuplicateMappings, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="{Binding DuplicateWarning}"
                           Foreground="White"/>
              </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
              <Button Content="+ Add Mapping"
                      Command="{Binding AddMappingCommand}"
                      Style="{StaticResource PrimaryButton}"
                      Margin="0,0,8,0"/>
              <Button Content="Clear All"
                      Command="{Binding ClearMappingsCommand}"
                      Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
          </Grid>
        </Border>
      </Grid>
    </Border>

    <!-- Footer -->
    <Border Grid.Column="0"
            Grid.ColumnSpan="3"
            Grid.Row="2"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="16"
            Margin="0,16,0,0">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal">
          <TextBlock Text="Data Profile Status:"
                     Foreground="{StaticResource SecondaryTextBrush}"
                     Margin="0,0,8,0"/>
          <TextBlock Text="{Binding ProfileStatus}"
                     Foreground="{StaticResource AccentBrush}"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal">
          <Button Content="Import CSV Headers"
                  Command="{Binding ImportHeadersCommand}"
                  Style="{StaticResource SecondaryButton}"
                  Margin="0,0,8,0"/>
          <Button Content="Export Data Profile"
                  Command="{Binding ExportProfileCommand}"
                  Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</UserControl>
